<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!-- ==========================================
         IDENTITÉ DU PROJET
         ========================================== -->
    <groupId>com.julio</groupId>
    <artifactId>reverso-web</artifactId>
    <version>1.0-SNAPSHOT</version>
    <packaging>war</packaging>

    <name>Reverso Web - CRM Clients/Prospects</name>
    <description>Application Jakarta EE - Gestion Clients et Prospects</description>

    <!-- ==========================================
         PROPRIÉTÉS GLOBALES
         ========================================== -->
    <properties>
        <java.version>25</java.version>
        <maven.compiler.target>25</maven.compiler.target>
        <maven.compiler.source>25</maven.compiler.source>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <!-- Versions centralisées — modifier ici = modifier partout -->
        <jakartaee.version>11.0.0</jakartaee.version>
        <tomcat.version>11.0.18</tomcat.version>
        <jstl.version>3.0.1</jstl.version>
        <mysql.version>8.3.0</mysql.version>
        <junit.version>5.13.2</junit.version>
    </properties>

    <!-- ==========================================
         DÉPENDANCES
         ========================================== -->
    <dependencies>

        <!-- Jakarta EE Web API (Servlet, JSP, EL)
             scope=provided : fourni par Tomcat, PAS dans le WAR -->
        <dependency>
            <groupId>jakarta.platform</groupId>
            <artifactId>jakarta.jakartaee-web-api</artifactId>
            <version>${jakartaee.version}</version>
            <scope>provided</scope>
        </dependency>

        <!-- JSTL — Tags JSP pour boucles, conditions dans les vues
             scope=runtime : pas besoin à la compilation, inclus dans le WAR -->
        <dependency>
            <groupId>org.glassfish.web</groupId>
            <artifactId>jakarta.servlet.jsp.jstl</artifactId>
            <version>${jstl.version}</version>
            <scope>runtime</scope>
        </dependency>

        <!-- MySQL Connector — connexion JDBC à la base de données -->
<!--        <dependency>-->
<!--            <groupId>com.mysql</groupId>-->
<!--            <artifactId>mysql-connector-j</artifactId>-->
<!--            <version>${mysql.version}</version>-->
<!--        </dependency>-->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-api</artifactId>
            <version>${junit.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-engine</artifactId>
            <version>${junit.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <!-- ==========================================
         PLUGINS DE BUILD
         ========================================== -->
    <build>
        <!-- Nom du fichier WAR généré → reverso-web.war -->
        <finalName>reverso-web</finalName>

        <plugins>

            <!-- Plugin compilateur Java 17 -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <source>${java.version}</source>
                    <target>${java.version}</target>
                    <encoding>UTF-8</encoding>
                </configuration>
            </plugin>

            <!-- Plugin WAR — exclut web.xml de la validation stricte -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-war-plugin</artifactId>
                <version>3.4.0</version>
                <configuration>
                    <failOnMissingWebXml>false</failOnMissingWebXml>
                </configuration>
            </plugin>

            <!-- Plugin Cargo — déploiement automatique sur Tomcat 10 -->
            <plugin>
                <groupId>org.codehaus.cargo</groupId>
                <artifactId>cargo-maven3-plugin</artifactId>
                <version>1.10.20</version>
                <configuration>
                    <container>
                        <containerId>tomcat11x</containerId>
                        <!-- Télécharge Tomcat automatiquement si absent -->
                        <artifactInstaller>
                            <groupId>org.apache.tomcat</groupId>
                            <artifactId>tomcat</artifactId>
                            <version>${tomcat.version}</version>
                        </artifactInstaller>
                    </container>
                    <configuration>
                        <type>runtime</type>
                        <properties>
                            <!-- Port HTTP de l'application -->
                            <cargo.servlet.port>8080</cargo.servlet.port>
                        </properties>
                    </configuration>
                </configuration>
            </plugin>

            <!-- Plugin JUnit 5 pour exécuter les tests -->
<!--            <plugin>-->
<!--                <groupId>org.apache.maven.plugins</groupId>-->
<!--                <artifactId>maven-surefire-plugin</artifactId>-->
<!--                <version>3.2.5</version>-->
<!--            </plugin>-->

        </plugins>
    </build>
</project>